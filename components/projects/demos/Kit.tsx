import { DemoFrame } from "./DemoFrame";
import type { DemoProps } from "../ProjectDemo";

/** Demo de "vibe100coding kit": stub. Uma tarefa futura preenche o conteúdo dentro da <DemoFrame>. */
export default function KitDemo({ label }: DemoProps) {
  return <DemoFrame name="vibe100coding kit" label={label} />;
}
